
const util = require('util')
require('./database')

const Role = require('./models/role')
const User = require('./models/user')

const run = async () => {
  console.log('ok')
  process.exit(0)
}

run()
